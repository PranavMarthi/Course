import math

pi = math.pi

radius = 3
circle_area = math.pi * radius**2
circle_circumference = math.pi*2*radius

print("A circle with radius of", radius, "cm has an area of", str(round(circle_area,2)) + "cm^2 and a circumference of",
      str(round(circle_circumference, 2)) + "cm.")
